import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
  title = 'hospitalV14Angular';
}

// login component ->  (Authentication module)
// radiologist component  (Radiology module)
// receptionist component, patient appointment component , appointment edit search filter comp
// ->  (Receptionist module  )
// pathologist component -> (pathologist module)
// admin component -> (admin module)
// doctor component -> (doctor module)
// 